.container.container-tiny
  = link_to '← My Combos', my_combos_path, :class => "btn btn-camouflaged mr10"
  = link_to 'Destroy', @combo, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-camouflaged mr10"
.container.container-tiny.tac
  %h1
    Editing Combo ##{@combo.id}
  = render 'shared/flash_messages'
#combo-form.container.container-tiny.card.mb20
  = render 'form_edit'
