#combo-generator-wrapper
  .modal-title
    %h2 Create your combo
  .modal-body
    #generator-controls.clearfix
      Add
      = number_field_tag :no_tricks, 2, in: 2..10
      tricks from
      #trick-list
        = select_tag "filter", options_for_select({ "The Database" => "database", "My Trick List" => "list" }, "list")
      %a.btn.btn-primary#add
        Add
    #generated-combos
      .combo.clearfix
        - unless @combo.nil?
          -# initialize all inputs based on this combo
          -# @combo.sequence.split(' > ').each do .trick-input
        - else
          .trick-input-container.linked
            %input.trick-input{}
            %a.remove X
            >
          .trick-input-container
            %input.trick-input{}
            %a.remove X
    #generator-actions.clearfix
      %a.btn.btn-success#save-combo
        Save
      %a.btn.btn-warning#discard
        Discard